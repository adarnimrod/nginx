---
- hosts: ansible-role-nginx-openbsd
  gather_facts: false
  roles: [openbsd_bootstrap]

- hosts: all
  roles:
      - ca-store
      - ansible-role-nginx

  post_tasks: # For testing.
      - name: Install curl
        package:
            name: curl
            state: present
